{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  // src/worker.ts is checked by tsconfig.worker.json instead: it needs the Workers globals, which
  // conflict with the DOM lib this config loads for the client script.
  "exclude": ["dist", "src/worker.ts"]
}
